import { AlterSourcePipe } from './alter-source.pipe';

describe('AlterSourcePipe', () => {
  it('create an instance', () => {
    const pipe = new AlterSourcePipe();
    expect(pipe).toBeTruthy();
  });
});
